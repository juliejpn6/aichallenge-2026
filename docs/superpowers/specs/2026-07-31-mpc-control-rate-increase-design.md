# MPC制御周期引き上げ(40Hz→72Hz)設計書

- 日付: 2026-07-31
- 対象パッケージ: `aichallenge/workspace/src/aichallenge_submit/multi_purpose_mpc_ros`
- 関連: design_docs/stage15_perf_20260707.html(時系列の全履歴)、課題棚卸しダッシュボード

## 1. 目的・背景

車速を今後36km/h程度まで引き上げていく計画に伴い、40Hzの制御周期では「1周期あたりに車両が進む距離」が速度に比例して増大し、対戦車状況の再評価・ENGAGE/STOPPING/switchback等の判断頻度が相対的に粗くなる。これは主に**反応速度**の課題であり、蛇行(AXIS06、限界サイクル振動)への効果は期待していない——蛇行の根本原因はアクチュエータの物理遅延(tau=190ms)とQ[e_y]チューニングの相互作用であり、40Hzは既にこの190ms系を制御するのに十分な速さ(約7.6周期/tau)であるため、サンプリング周期を上げても物理遅延そのものは変わらない。

### 目標値の算出根拠

現在の運用速度(v_max=20km/h)における「1周期あたりの走行距離」を、目標速度36km/hでも維持する:

```
新周期 = 40Hz × (36km/h ÷ 20km/h) = 72Hz
```

## 2. スコープ

### 対象
- `control_rate`をconfig.yamlで72.0Hzへ変更可能にする(値自体の変更は本設計の実装完了後、実測確認を経て行う)
- `control_rate`に依存する全ての周期数ベース閾値・EMA/ローパスゲインを、周期を変えても実時間の意味を保つよう自動換算する仕組みを導入する
- 診断ログ(`[PERF]`)のハードコードされた25ms閾値を修正する

### 非対象(明示的に対象外)
- AXIS06(蛇行/限界サイクル)の根本対処——別スレッドの課題として継続
- MPCホライズン自体の変更——`core/MPC.py`のホライズンはwaypoint間隔(空間ベース、`delta_s = wp_next - wp`)で離散化されており、`control_rate`とは独立している。本設計では変更しない
- ローカリゼーション(EKF)側のプロセスノイズ等——別サブシステム、本設計のスコープ外

## 3. 現状分析: control_rate依存箇所の分類

`config.yaml`・`mpc_controller.py`・`lateral_ttc_monitor.py`を網羅的に調査し、4カテゴリに分類した。

### カテゴリA: 周期数(整数カウント)閾値 — 線形換算が必要

`新値 = round(旧値 × control_rate / 40.0)`

| パラメータ | 現在値(40Hz基準) | 実時間 | 定義箇所 |
|---|---|---|---|
| `hold_cycles` | 60 | 1.5s | config.yaml:427(STUCK検知) |
| `gear_settle_cycles` | 20 | 0.5s | config.yaml:432 |
| `stall_hold_cycles` | 400 | 10.0s | config.yaml:453 |
| `infeas_thr` | 300 | 7.5s | config.yaml:428 |
| `ghost_block_hold_cycles` | 40 | 1.0s | config.yaml:525 |
| `giveup_cycles` | 40 | 1.0s | config.yaml:548 |
| `engage_debounce` | 8 | 0.2s | config.yaml:549(6箇所以上で共有) |
| `engage_cooldown` | 160 | 4.0s | config.yaml:567 |
| `def_enter_cycles` | 5 | 0.12s | config.yaml:589 |
| `def_exit_cycles` | 15 | 0.38s | config.yaml:590 |
| `unlock_inf_cycles` | 80 | 2.0s | config.yaml:593 |
| `unlock_hold_cycles` | 60 | 1.5s | config.yaml:594 |
| `collision_cum_window_cycles` | 5 | 0.125s | config.yaml:521 |
| `min_trend_cycles` | 3 | 0.075s | config.yaml:397(LAT-TTC) |
| `infeasible_latch` | 40 | ≈1.0s | config.yaml:653 |
| `course_in_count` | 5 | ヒステリシス | config.yaml:693 |
| `osqp_shadow_cycles` | 50 | 起動時のみ | config.yaml:329 |
| `_pf_report_every`(コード内リテラル) | 400 | 10s | mpc_controller.py:4186 |

**例外**: `shuffle_max_cycles`(config.yaml:497)は試行回数のカウントであり、周期時間ではないため換算対象外。

### カテゴリB: 減衰係数型(EMA/ローパスゲイン) — 指数換算が必要

`新値 = 1 - (1 - 旧値) ** (40.0 / control_rate)`(単純な線形換算では時定数がずれる)

| パラメータ | 現在値 | 定義箇所 |
|---|---|---|
| `ema_alpha` | 0.05 | config.yaml:564 |
| `r_delta_swing_ema_beta` | 0.15 | config.yaml:105 |
| `beta`(LAT-TTC v_inst平滑化) | 0.15 | config.yaml:388付近 |
| `accel_low_pass_gain` | 0.35 | config.yaml:129 |
| `steer_low_pass_gain` | 0.35 | config.yaml:138(蛇行対策で慎重にチューニング済み) |

### カテゴリC: ハードコードされた実時間定数 — 直接修正が必要

- `mpc_controller.py:4226` `if work > 0.025:` — 40Hzの周期(25ms)を直書き。`1.0 / control_rate`を参照するよう修正する。

### カテゴリD: 既に実時間ベースで安全(変更不要、確認のみ)

- `backup_timeout_s`/`push_timeout_s`(252節で実時間判定と確認済み)
- `ttc_danger_s`/`ttc_critical_s`
- `dt = (now - self._last_t).nanoseconds / 1e9`(実測経過時間)
- `self._loop % int(self._mpc_cfg.control_rate)`系の間引きロジック(既に動的参照)
- MPCホライズン本体(空間ベース)
- `_lag_step = 0.025`(mpc_controller.py:2425、V2X予測の内部積分刻み。control_rateと無関係な独立パラメータ)
- **操舵スルーレートリミッタ**(`core/MPC.py:648`、`max_delta_change = self.max_steering_rate * self.model.Ts`)——Geminiレビュー(Phase 0調査項目1)で懸念された「周期あたり最大変化量」だが、`self.model.Ts`は`mpc_controller.py:525`で`Ts = 1.0 / self._cfg.mpc.control_rate`と既にcontrol_rateから動的に計算されている。したがって`max_delta_change × control_rate = max_steering_rate`(不変)が常に成立し、**1秒あたりの実効レート上限はcontrol_rateを変えても自動的に一定に保たれる**。新規のカテゴリE(逆比換算)は不要と判断した
- `collision_cum_window_cycles`が対象とする累積量(`_collision_v_window`等、mpc_controller.py:4451)は生の実速度値(m/s)であり、「周期ごとの増分」ではない。この量自体はレート非依存の物理量であり、窓長(周期数)をカテゴリAとして線形換算するだけで正しく実時間の意味を保てる(追加対応は不要)
- OSQPソルバー設定(`core/MPC.py:402,462`の`.setup()`呼び出し)は`max_iter`/`time_limit`を明示的に指定しておらずライブラリ既定値に依存している。72Hzの13.9ms予算と直接コンフリクトする設定は見つからなかったが、逆に言うと「予算内で強制打ち切る」安全網はソルバー側に元々存在しない(72Hz化固有の新規リスクではなく、既存のリスクがそのまま残る)

### 副次的な影響(正・負の両方、実測で確認が必要)

- **正の可能性**: 72Hz化で1周期あたりの状態変化(位置・姿勢・障害物配置)が相対的に小さくなるため、OSQPのウォームスタート初期値の精度が上がり、反復回数・solve時間が短縮される可能性がある(未検証、実測で確認)
- **負の可能性**: `/control/command/control_cmd`等の発行頻度が1.8倍になることで、下流(ログ収集・可視化・他ノードのSubscriber)側のQoS/Queueサイズが不足し、メッセージ溢れ・可視化の取りこぼしが起きる可能性がある(本パッケージ内のsubscriptionは全てdepth=1のKEEP_LASTで確認済みだが、下流の他ノード側は未調査)
- 入力トピック(EKF自己位置・V2X対戦車認識)の実際の更新レートは本リポジトリのソースからは確認できなかった(GNSS生値は既存ドキュメントで20Hzと判明しているが、EKF出力・V2X配信レートはAutoware本体側の設定に依存すると見られる)。72Hzで再計算しても、これらの入力がより低頻度でしか更新されない場合、一部の周期は同一のセンサ値を再利用するだけになり、期待するほどの反応速度向上が得られない可能性がある。実測で確認する

## 4. 採用する実装方針: 明示的ヘルパー + デバッグログ

config.yamlの数値・コメントは一切変更しない(「40Hz基準で書かれた値」という意味を保つ)。読み込み側で以下2つのヘルパーを新設し、カテゴリA・Bの各読み込み箇所で明示的にラップする。

```python
_RATE_SCALE_REFERENCE_HZ = 40.0  # config.yamlの数値は全てこの基準周期で書かれている

def _rate_scaled_cycles(self, name: str, cycles_at_ref: int) -> int:
    """周期数閾値をcontrol_rateに合わせて線形換算する(カテゴリA用)。
    config.yamlの値は_RATE_SCALE_REFERENCE_HZ(40Hz)基準で書かれているため、
    実際のcontrol_rateとの比で換算し直す。起動時に換算前後の値をログする。
    max(1, ...)は、将来control_rateを大きく下げた場合に閾値が0(=即発火)へ
    潰れる事故を防ぐガード(Geminiレビュー指摘、2026-07-31反映)。"""
    scaled = max(1, round(
        cycles_at_ref * self._mpc_cfg.control_rate / self._RATE_SCALE_REFERENCE_HZ))
    self.get_logger().info(
        f"[RATE-SCALE] {name}: {cycles_at_ref}周期@{self._RATE_SCALE_REFERENCE_HZ:.0f}Hz "
        f"({cycles_at_ref / self._RATE_SCALE_REFERENCE_HZ:.3f}s) "
        f"-> {scaled}周期@{self._mpc_cfg.control_rate:.0f}Hz")
    return scaled

def _rate_scaled_gain(self, name: str, gain_at_ref: float) -> float:
    """EMA/ローパスゲインをcontrol_rateに合わせて指数換算する(カテゴリB用)。
    離散一次遅れの時定数を保つ式(1-(1-gain)^(ref_rate/rate))を使う
    (単純な線形換算(gain×ref_rate/rate)では時定数がずれるため不可)。"""
    scaled = 1.0 - (1.0 - gain_at_ref) ** (
        self._RATE_SCALE_REFERENCE_HZ / self._mpc_cfg.control_rate)
    self.get_logger().info(
        f"[RATE-SCALE] {name}: gain={gain_at_ref:.4f}@{self._RATE_SCALE_REFERENCE_HZ:.0f}Hz "
        f"-> {scaled:.4f}@{self._mpc_cfg.control_rate:.0f}Hz")
    return scaled
```

呼び出し例:

```python
self._stuck_hold_cycles = self._rate_scaled_cycles("hold_cycles", int(_stkget("hold_cycles", 60)))
self._steer_low_pass_gain = self._rate_scaled_gain("steer_low_pass_gain", float(_mpcget("steer_low_pass_gain", 0.35)))
```

起動時に約23行の`[RATE-SCALE]`ログが出力され、換算前後の値を目視確認できる。`shuffle_max_cycles`等の換算不要な値はヘルパーを呼ばないことで自然に除外される。

### この方針を選んだ理由

- 既存コードの慣用パターン(`_room_to_wall`・`_vid_changed_reset`等、命名規則による自動判定ではなく呼び出し箇所ごとに明示的にヘルパーを呼ぶ流儀)に整合する
- config.yamlのスキーマ変更(キー名の秒単位への一括改名)を避けられ、既存のdesign_docs記述・コメントとの整合を壊さない
- `control_rate`を変更するだけで全ての依存値が自動追従するため、将来の周期変更(例: 72→100Hzへの再挑戦、デバッグ用に40Hzへ戻す)が構造的に安全になる

## 5. テスト方針

- 新規: `_rate_scaled_cycles`/`_rate_scaled_gain`の単体テスト(数式の正しさ、丸め挙動、境界値、ログ出力の存在、`_RATE_SCALE_REFERENCE_HZ`定数の再利用確認)
- 既存テストのうち、カテゴリA・Bの「読み込み後の実値」(`self._stuck_hold_cycles`等)をリテラルで検証しているものは、72Hz環境下での期待値(例: 60→108)に更新する(弱化ではなく追随)
- 回帰スイート全体を実行し、PASSを確認する

### 丸め誤差チェックリスト(Geminiレビュー指摘、2026-07-31追加)

カテゴリAの線形換算(`round(旧値 × 新rate / 旧rate)`)は、値が小さいほど丸め誤差による実時間の相対的なズレが大きくなる。特にデバウンス・ヒステリシス系の小周期値は、安全側に倒れているか(=誤検知を増やす方向のズレでないか)を目視確認する:

| パラメータ | 40Hz時 | 72Hz換算値(丸め後) | 実時間換算(真値) | 相対誤差(真値比較) |
|---|---|---|---|---|
| `min_trend_cycles` | 3 | 5(=3×1.8=5.4→5) | 0.0694s(真値0.075s比 -7.4%) | 短くなる方向(誤発火防止が僅かに緩む) |
| `def_enter_cycles` | 5 | 9(=9.0、丸め無し) | 0.125s(真値0.125s比 誤差なし) | 誤差なし |
| `collision_cum_window_cycles` | 5 | 9(=9.0、丸め無し) | 0.125s(真値と一致) | 誤差なし |

**2026-07-31再訂正(Geminiレビュー指摘)**: `def_enter_cycles`の当初の「+4.2%」という記載は誤りだった。40Hz時の真値(5/40=0.125s)をconfig.yamlのコメント(「≈0.12s」という丸め表示)と比較してしまっていたのが原因で、真値同士(0.125s vs 0.125s)を比較すれば誤差はゼロである(`def_enter_cycles`は5×1.8=9.0と割り切れるため、この項目には本来丸め誤差は一切発生しない)。実際に丸め誤差が生じるのは`min_trend_cycles`(3×1.8=5.4→5)のように、40Hz時の値と1.8の積が整数にならない場合のみである。

`min_trend_cycles`のように丸めが切り捨て方向(短くなる)に働く場合、意図した誤発火防止時間より僅かに早くTTC評価が始まる。この程度の誤差(-7.4%)は許容範囲と判断するが、実装後に`[RATE-SCALE]`ログで全対象の丸め方向を確認し、安全側に倒れていないものがあれば個別に検討する。

## 6. ロールアウト・検証手順

1. 本設計の実装(ヘルパー新設+23箇所のラップ+`[PERF]`ハードコード修正)を40Hzのまま(`control_rate`は変更せず)完了させ、`[RATE-SCALE]`ログが全て「換算前後で同じ値」(40Hz→40Hzなので変化なし)になることを確認する(①非矛盾性: 既存挙動が一切変わらないことの確認)
2. `control_rate`を72.0へ変更し、`[RATE-SCALE]`ログで想定通りの換算値(表の「72Hz換算後」列と一致)になっていることを確認する
3. 実運用環境(予選環境)で処理落ち率を実測する。現状40Hzでの実測平均処理時間は10.2ms(25ms予算の約40%)。72Hzでは1周期の予算が13.9msとなり、平均処理時間がほぼ変わらないと仮定すると使用率は約72%——スパイク(過去に平常の3〜5倍を記録した事例あり)への耐性は不明であり、この実測が本設計の成否を分ける
4. **判定基準(Geminiレビュー指摘、2026-07-31追加)**: 10分間以上の連続走行ログで、1周期の処理時間が13.9msを超えた回数の割合が**0.1%以下**であればPass、それを超えればFailとする。**さらに「最大連続超過数 ≦ 3周期」を併記する**(2026-07-31再追加、Geminiレビュー指摘)——散発的な単発超過と、3周期以上連続する超過は安全上の意味が異なる(後者は実際の反応遅延として体感されうる)ため、超過率が0.1%以下でも連続超過が続く場合は追加調査の対象とする
5. **段階的フォールバック**: 4の判定がFailの場合、まず`control_rate`を**60Hz(予算16.66ms、想定使用率約61%)**へ下げて同じ基準で再測定する。60Hzでも基準を満たさない場合は、コリドー計算(現状最重量、平均4.01ms)の最適化を別課題として着手した上で72Hz(または60Hz)への再挑戦を検討する。`control_rate`を変更するだけで全ての依存値([RATE-SCALE]ログ経由)が自動追従するため、この段階的な後退・再測定は追加の実装変更なしに行える
6. **実測開始前の前提条件確認(2026-07-31追加、Geminiレビュー指摘)**: 手順3の実測を始める前に、`ros2 topic hz /localization/kinematic_state`・`ros2 topic hz /v2x/vehicle_positions`でEKF自己位置・V2X認識トピックの実際の更新レートを確認する(3章「副次的な影響」で触れた通り、これらが72Hzより低頻度の場合、期待する反応速度向上が得られない可能性があるため)。また、rosbag記録系が1.8倍のメッセージ量(`/control/command/control_cmd`等)を取りこぼさずに記録できることも確認する
7. **タイマーのオーバーラン挙動に関する脚注(2026-07-31追加、Phase 0調査結果)**: 制御ループは`create_timer`のコールバック方式ではなく、`run()`内の`while rclpy.ok(): self._control()`ループ+`rclpy.Rate.sleep()`で駆動されている。したがって、上記4の「周期超過」は**コールバックのスキップや欠落を意味しない**——1周期の処理が予算を超えても、その周期の実行自体は必ず最後まで完了し、次の周期がその分だけ後ろ倒しで開始されるだけである(処理落ち=「制御が飛ぶ」ではなく「制御が遅延する」)。判定基準の数値はこの前提の下で解釈すること

8. **dt分布ベースの判定基準を併記する(2026-07-31追加、クローズ作業Phase 3)**: 手順7の脚注の通り、「周期超過」は処理時間(work)だけでなく`rclpy.Rate.sleep()`自体のジッタも含めた「実際に何秒おきに`_control()`が呼ばれたか」(dt、`_control()`呼び出し開始時刻の連続差分)で評価する必要がある。そのための専用計装`[PERF-DT]`(`_dtperf_record`、既存の`_pf_report_every`窓・`self._pf_over_budget_s`をそのまま再利用、新規パラメータなし)を実装した。上記4の処理時間ベース基準に加え、以下をAND条件として満たすことをPassの条件とする:
   - **実効平均レート ≧ 70Hz**(`[PERF-DT]`の`eff_rate`、目標72Hzの約97%。集計窓の周期数÷経過実時間)
   - **dtのp99 ≦ 予算+J(加算ジッタモデル、2026-08-01改訂。旧基準「予算×1.5」は下記コラム参照)**
   - **dt>予算の連続回数 ≦ 3周期**(4の連続超過基準と同じ閾値を、処理時間ではなくdt定義で読み替えたもの。`[PERF-DT]`の`max_consec_over`)
   処理時間ベースの基準(4)とdt分布ベースの基準(本項目)のどちらか一方でも不合格ならFailとする(両方が独立に重要な情報を持つため、いずれかで代替しない)

   > **dt p99基準の改訂(2026-08-01、262節続報、判定基準改訂+work_cpu計装Phase 3)**: 旧基準「予算×1.5」(72Hzなら20.85ms)は、予算に対する**倍率**でジッタ許容量を決めるモデルだった。しかし新プラットフォーム状態(governor=performance)での40Hzベースライン再測定(Phase 4、work>予算0.000%=完全健全な状態)で、dt p99が42.54ms——**40Hzの予算25ms×1.5=37.5msすら超過**していることが判明した。すなわち旧基準は「処理が完全に健全な40Hzですら不合格になる」壊れた検査であり、72Hzだけを不当に厳しく評価していたわけではなかった。
   >
   > 原因は、`rclpy.Rate.sleep()`のジッタ(OSスケジューラの起床精度限界)が**予算に比例するのではなく、ほぼ一定の絶対量**として乗ることにある。40Hzのジッタ量 J=(dt p99−予算)≈42.54−25.0=**17.54ms**は、72Hzの実測(C0: 28.43ms≈13.9+14.5、C1: 28.84ms≈13.9+14.9)と同じオーダーで整合する——予算(周期長)が変わってもジッタの絶対量はほぼ変わらないという加算モデルの方が実測に整合する。
   >
   > このため基準を「予算×1.5」から「**予算+J**」(Jは同一プラットフォーム条件での40Hzベースライン実測から都度求める値)へ改訂する。**これは基準を甘くする変更ではなく、実測に基づいて壊れていた検査を正す変更である**——40Hzで既に健全としてPassしていた運用を後付けで正当化するために基準を緩めたのではなく、40Hzが本来Passすべき状態であるにもかかわらず旧基準では誤ってFail相当の数値になっていたことを直接実測で確認した上での改訂である。J自体はプラットフォーム条件(governor等)ごとに変わりうるため、条件を変えるたびに40Hzベースラインで再測定すること。
   >
   > **この改訂を現時点のJ≈17.5ms(72Hz基準≈31.4ms)で過去の実測(C0/C1/C2)に適用し直すと**、C0(p99=28.43ms)・C1(p99=28.84ms)は共に新基準を満たす。C2のみ(p99=33.56ms)僅かに超過する。つまり**dt p99基準は実質的にC0/C1では既に達成されていた**——残る唯一の未達基準は「work>予算の割合(目標≦0.1%)」であり、問題はスケジューリングジッタではなく、work(壁時計処理時間)側の残存スパイクに絞り込まれたことになる。
9. **40Hzベースラインの取得(2026-07-31追加、クローズ作業Phase 3)**: 手順1(40Hz恒等確認)を実施する際、`[PERF-DT]`のログも合わせて1回分採取し、`rclpy.Rate.sleep()`自体が40Hzで実際にどの程度のジッタを持つか(dtのp50/p95/p99/max、実効平均レート)をベースラインとして記録しておくこと。72Hz時の評価(手順8)は、この40Hzベースラインとの相対的な悪化度合いも合わせて確認する(40Hz自体が既に想定より大きいジッタを持っていた場合、72Hz時の悪化を過大評価しないため)
10. **rclpy.Rate精度の確認(2026-07-31追加、クローズ作業Phase 3)**: 13.9ms(72Hz)という短い周期での`rclpy.Rate.sleep()`のジッタは、40Hz(25ms周期)の実績からは外挿できない(OSスケジューラの分解能・スリープ精度の非線形性による)。したがって、72Hz切替直後の**最初の確認項目**として、`[PERF-DT]`のdt分布(特にp99・max)を確認し、処理時間(work)がほぼゼロに近い早い段階のログでも顕著なジッタが見られる場合は、負荷とは独立した`rclpy.Rate`自体の精度限界の可能性を疑うこと

11. **dt超過判定へのマージン(ε)導入と較正手順(2026-08-01追加、72Hz切替準備Phase 1)**: `rclpy.Rate.sleep()`の起床ジッタにより、健全な周期でもdtは予算を中心に上下へ散らばる。マージン無しの「dt>予算」判定は、ジッタが予算の内側で収まっていても約半数の周期で真になりうる誤検出を招く。そこで`_dtperf_record`の超過判定を「`dt > 予算 + ε`」へ改めた(`config.yaml`の`mpc.perf_dt_over_margin_ms`、既定0.0=旧挙動と完全に同一、レートスケーリングの換算対象外)。

    **ε=85ms(2026-08-01確定、根拠: 下記2回の実機ベースライン実測)**。

    *①単一車両ベースライン*: AWSIM単体走行(対戦車無し、`make dev`)で約10.7分(64窓・25,600周期)の`[PERF-DT]`ログを実採取した結果:
    - dtのp50/p95/p99(64窓平均): 24.32ms / 36.55ms / 49.01ms。窓ごとのp99は最悪37.04〜68.43msの範囲
    - マージン無し(0ms)でのover_budget率: 平均49.23%(46.75〜50.50%) — **健全な40Hz走行でもほぼ半数の周期がマージン無しでは「超過」と誤検出される**ことを実測で確認
    - 同区間の`[PERF]`(処理時間work): avg 8.55ms、>25ms該当は400周期中平均3.27回のみ — dtの超過率(約49%)との乖離が、超過の主因が処理落ちではなく`rclpy.Rate`自体のジッタであるという仮説を裏付けた
    - 除外事象: 区間中1回だけmax dt=179.34msの外れ値を観測(起動2窓目、シミュレータ起動直後の一過性トランジェントと判断し較正の母集団から除外。この単発事象ごと吸収するほどεを緩めると真の劣化検出感度が落ちるため)

    *②対戦車あり2両編成ベースライン*(ユーザー指示により追加実施、`make dev2`、各車約12分・71〜72窓): phaseA(コリドー計算)が対戦車のラスタライズ負荷で①の2倍程度(avg 5-6ms、①は2-3ms)に増加し、窓ごとp99は最悪89.30ms(d2、budgetとの差=64.30ms)まで悪化した。①のε=55msを適用した状態でこの②を走らせたところ、over_budget率は0.3〜0.5%まで低下しており(参考値)、55msでも一定の効果はあったが、②の方が①より明確に負荷が重いため、より保守的な②基準で再較正した: 窓ごとp99の最悪値と予算の差(64.30ms)に1.3倍の余裕を掛けた83.6msを丸め、**ε=85msへ改定**した。

    なお②ではphaseB(組み合わせ探索)は一度も起動しなかった(対戦車ありでもegoの前方視線が複数区間に分断される幾何条件が今回の走行では発生しなかったため)。合成ゴールデン/ファジングケース(256/258節)が引き続きphaseB性能の唯一の実測根拠であることに変わりはない。
12. **合否判定は全区間(10分)集計で行う**(2026-08-01追加): 400周期窓ごとの`[PERF-DT]`出力はボトルネック箇所の特定(いつ・どの区間で悪化したか)に使い、Pass/Failの判定そのものは走行区間全体を通した集計値(over_budget%・max_consec_over・p99・eff_rate)で行うこと。窓ごとの値だけを見て早期にPass/Failを断定しない
13. **eff_rateは主指標にしない**(2026-08-01追加): 実効平均レート(`eff_rate`)は、一度発生した遅延を後続周期が追いつく(キャッチアップする)ことで平均化されてしまう鈍い指標であり、単独では健全性の根拠にしない。検出の主力はp99と連続超過回数(`max_consec_over`)であり、`eff_rate≧70Hz`はAND条件の一角(補助的な確認)に留める
14. **dtのクロックソース(2026-08-01追加)**: `[PERF-DT]`が使うdtは、既存の制御ロジックが使うdt(`self.get_clock().now()`由来、`use_sim_time:=true`のAWSIM接続時は`/clock`トピック由来のシミュレーション時刻)とは独立に、`time.perf_counter()`(常に壁時計)で計測する。AWSIMのレンダリング負荷等でシミュレーションが壁時計の1倍速で進まない場合、シミュレーション時刻ベースのdtは実際の`rclpy.Rate.sleep()`のジッタを反映しなくなり、72Hz予算との比較が歪むため。制御ロジックが使う既存のdt自体には一切手を加えていない
15. **`cache_builds`と`[PERF-CORRIDOR]` phaseA maxの相関確認(2026-08-01追加、258節続報)**: `update_by_topic`有効構成のベースライン計測では、`[PERF-CORRIDOR]`の`cache_builds`カウンタ(100周期ごとのreference_path再構築直後にホライズン内N個分のバーストが正常として発生する、258節参照)と、同じ周期のphaseA maxの相関を確認すること。もしこのバーストがphaseA maxのスパイクと有意に相関し、かつそのスパイクが予算に対して無視できない大きさである場合、対処の第一候補は`ReferencePath.__init__`自体の変更(プリウォーム化、258節Phase 4-2で見送り済み)ではなく、**コントローラ側の再構築ハンドラ(100周期ごとのコードパス)で新ReferencePath構築直後に全waypointキャッシュを一括構築する**方式とする(現時点では未実装、必要になった場合の設計方針としてここに記録するのみ)
16. **プラットフォームチェックリスト(2026-08-01追加、262節続報、プラットフォーム状態計装Phase 1)**: 72Hz実測(2026-08-01初回)が不合格となった後、原因調査中にユーザーから「実測時にOSの電力プラン(省電力/バランス設定)がかかっていた可能性があり、高パフォーマンスへ変更して解除した」との情報提供があった。この種の環境要因は`[PERF]`/`[PERF-DT]`だけでは追跡できないため、`[PERF-SPIKE]`発火行と`[PERF-RUSAGE]`窓集計に**CPU周波数(`scaling_cur_freq`、全コアの平均・最小・最大、単位MHz)**を追加し、ノード起動時には`[PERF-PLATFORM]`として`scaling_governor`・`scaling_max_freq`・(取得できる場合)RAPL電力上限を1回記録するようにした(実装: `mpc_controller.py`の`_pf_sample_cpu_freq`/`_pf_log_platform_checklist`、テスト: `test_cpu_freq_instrumentation_262.py`)。**以後の実測はこのプラットフォームチェックリスト付きでのみ有効とする**——電力プラン等の環境要因を実測条件から排除しないまま比較すると、ソフトウェア側の改善効果と環境側の改善効果を取り違える(交絡)ため。sysfsのcpufreqがコンテナから不可視な環境では全フィールドがN/A表示になり、その場合はホスト側での並行記録(別途スクリプト)が必要になる

### 実測記録

> **判定の二段構え(2026-08-01追加、262節続報)**: 以下の実測はすべて開発PC(ローカル)で行っている。**開発PCでの合格は「次(予選環境での実測)へ進む根拠」に過ぎず、72Hz採用そのものの根拠にはならない**。予選環境(本番計算機)は開発PCとCPU・冷却・電源制御の全てが異なるため、採用可否の最終判断は予選環境(または少なくとも同等スペックの実機)での合格をもって行う。本節の実測記録は、この二段構えの第一段(開発PCでの絞り込み)の記録として扱うこと。

**2026-08-01: 72Hz実測①初回(単一車両、約9.7分・58窓・41,760周期、`make dev`) — 不合格**

> **プラットフォーム脚注(2026-08-01追加、262節続報)**: この実測はOSの電力プラン(省電力/バランス)が有効なまま行われた可能性が高い(実測後にユーザーから「電力プランを高パフォーマンスへ変更して出力制限を解除した」との情報提供があった)。項目16のプラットフォームチェックリスト計装は本実測より後に追加したため、この回にはガバナー/周波数の直接記録が無い。40Hz時代に観測されていた「平常の3〜5倍」のスパイク事例も、一部はこの電力制限に由来していた可能性がある(未検証の推測として記録するのみ)。

| 基準 | 目標 | 実測 | 判定 |
|---|---|---|---|
| 処理時間: work>13.9msの割合 | ≦0.1% | 6.41%(2,677/41,760周期) | ❌ 不合格(64倍) |
| dt: p99 | ≦20.85ms(予算×1.5) | 平均34.95ms・最悪41.84ms | ❌ 不合格(58/58窓すべて未達) |
| dt: 連続超過(dt>予算+ε=85ms) | ≦3周期 | 0(全窓) | ✓ 合格 |
| eff_rate | ≧70Hz | 平均68.69Hz(65.65〜71.02Hz) | △ 51/58窓(87.9%)が未達 |

原因の切り分け: work avg=9.17ms(予算の約66%)自体は健全で7章の事前見積もり(約72%)とほぼ整合していた。不合格の主因はwork maxのスパイク(平均45.41ms・最悪65.30ms、予算の3〜4.7倍)で、主要寄与源は`mpc_setup`(avg 6.8-6.9ms・max 38.8-42.9ms)と`mpc_corridor`(avg 4.1ms・max 36.6-38.9ms)。ε(dt>予算+ε)の仕組み自体は正しく機能した(連続超過基準は合格)——不合格の原因はrclpy.Rate自体のジッタではなく処理時間スパイクであり、εでは吸収できない別種の問題である。

対応: `control_rate`を40.0へ復帰し、スパイクの根本原因調査(4容疑者: ①100周期ごとのreference_path再構築バースト、②AWSIM同居によるCPU競合、③Python GC第2世代、④OSQPフルセットアップ再実行)へ着手した。詳細は design_docs/stage15_perf_20260707.html 260/261節続報を参照。

**2026-08-01: 72Hz実測②再実測(電力プラン=高パフォーマンスへ変更後、単一車両、約9分、`make dev`) — 不合格(改善はしたが未達のまま)**

`[PERF-SPIKE]`計装(261節続報Phase 3)を実装した上で、電力プランを高パフォーマンスへ変更してから再実測した。

| 指標 | ①初回(電力制限下) | ②再実測(解除後) | 変化 |
|---|---|---|---|
| work avg | 9.17ms | 6.09ms | -34% |
| work max(窓平均) | 45.41ms | 24.35ms | -46% |
| work>13.9ms割合 | 6.41% | 0.823% | 約1/8(目標0.1%には未達❌) |
| dt p99(窓平均) | 34.95ms | 28.43ms | -19%(目標20.85msには未達❌) |
| dt max(窓平均) | 57.50ms | 39.61ms | -31% |
| 連続超過(dt>予算+ε=85ms) | 0 | 0 | ✓合格維持 |
| eff_rate | 68.69Hz | 70.43Hz | ✓新規合格(70Hz基準を上回った) |

4基準中2基準(連続超過・eff_rate)が新規合格、残り2基準(work>予算割合・dt p99)は大幅改善したが依然未達のため、総合判定は不合格のまま。

`[PERF-SPIKE]`による帰属分析(実発火33件、約9分間、`nseg`不変で仮説(d)排除・`cache_builds_diff`常に0で仮説(a)排除・`gen2_gc`常にFalseで仮説(c)排除): 唯一残った容疑は仮説(b)系統で、`nivcsw_diff`の上昇(スパイク時5〜25、平常時平均3.45を上回る)から「CPU競合」が示唆されたが、この実測回にはCPU周波数の直接記録が無く、**「競合」と「電力プランによる周波数低下(DVFS)」を計測上は区別できていない**(電力プラン変更で改善したという事実自体が後者の関与を示唆する)。この空白を埋めるため項目16のプラットフォームチェックリスト計装(CPU周波数・governor記録)を追加し、Phase 3で両者を実験的に切り分ける。詳細は design_docs/stage15_perf_20260707.html 262節続報を参照。

**2026-08-01: Phase 3介入実験(C0/C1/C2、単一車両、`make dev`) — governor固定は微改善、cpuset隔離は悪化**

項目16のプラットフォームチェックリスト計装を使い、C0(無介入、②の再掲)・C1(`cpupower frequency-set -g performance`でgovernor固定)・C2(C1+`docker-compose.cpu-pin-experiment.yml`でautowareを論理CPU0-3、simulatorを論理CPU4-15へcpuset隔離、SMT兄弟スレッドも分離)の3条件を実測した。C1/C2はターン間の待機時間の都合で当初想定(10分)より大幅に長く実行され、それぞれ約49.7分・約4時間17分のデータが取れた(意図せぬ長時間データだが、統計的にはより頑健)。

| 指標 | 目標 | C0(無介入、約9分) | C1(governor=performance、約49.7分) | C2(C1+cpuset隔離、約4時間17分) |
|---|---|---|---|---|
| work>13.9ms割合 | ≦0.1% | 0.823% | 0.684% | **0.917%(最悪)** |
| dt p99(窓平均) | ≦20.85ms | 28.43ms | 28.84ms | **33.56ms(最悪)** |
| 連続超過 | ≦3周期 | 0 | 0 | 0 |
| eff_rate | ≧70Hz | 70.43Hz(✓) | 70.58Hz(✓) | **62.17Hz(❌新規不合格)** |
| nivcsw/720周期窓平均 | (参考) | (未計測) | 2304.4 | **4268.3(約1.9倍)** |
| `[PERF-SPIKE]`発火頻度 | (参考) | 33件/9分≈3.7件/分 | 37件/49.7分≈0.74件/分 | 272件/257分≈1.06件/分 |
| freq_avg(窓平均) | (参考) | (未計測) | 3749MHz | 3477MHz |

帰属分析: C1〜C2いずれも全発火で仮説(a)(c)(d)は排除(`cache_builds_diff`はC2の272件中271件が0、1件のみ20——後述)・`gen2_gc`は全件False・`nseg_changed`は全件False。

- **C1(governor固定)**: work>予算割合・`[PERF-SPIKE]`発火頻度(3.7件/分→0.74件/分、約1/5)は改善したが、dt p99・eff_rateはC0とほぼ同水準に留まり、4基準の合否自体は変わらなかった(連続超過・eff_rateの2基準合格は維持、他2基準は未達のまま)。DVFS(周波数低下)は severe spike の一部には寄与していたが、残存する軽度〜中程度の超過やdt p99の高さの主因ではないことが示唆される。
- **C2(cpuset隔離追加)**: 全指標がC0/C1より悪化し、特にeff_rateが70Hz基準を新規に割り込んだ(62.17Hz)。`autoware`コンテナは`mpc_controller`単体ではなく`autostart_orchestrator`・`v2x_marker_publisher`・`rviz2`・`ekf_localizer`・`gnss_poser`等**多数のROS2ノードを同居**させている。`cpuset`無指定時は`cpus:4`のCFSクォータを保ちながら16論理CPUの空いている物理コアへ自由に散らばれるが、`cpuset=0-3`(論理CPU4個=物理コア2個)へ縛ると、これら複数プロセス間で**同じ2物理コアのSMT兄弟スレッドを奪い合う内部競合**が発生したと考えられる(nivcsw倍増・eff_rate急落と整合)。simulatorとの外部競合を減らす意図だったが、コンテナ内の複数ノード間競合という別種の負荷を新たに生んでしまい、狙った効果を得られなかった。この設計(コンテナ全体を4論理CPUへ縛る)は**不採用**とする。
- **想定される次の一手**: 「コンテナ全体」ではなく「`mpc_controller`プロセス単体」により多くの専有コアを割り当てる(例: taskset的に他ノードとは別のコアへ)方式であれば、内部競合を避けつつ外部競合も減らせる可能性がある。ただしこれはコンテナのcpusetという粗い単位では実現できず、コンテナ内プロセスへの個別`taskset`(またはコンテナ分割)という、より侵襲的な変更が必要になる。**C3としてPhase 4で実装・Phase 5で実測する**(下記)。
- **C2実測時間の非対称性についての注記(2026-08-01追加)**: C2は当初、実行途中でマシンが再起動しX11切断によりクラッシュ→再実行という経緯を辿ったが、**上表のC2データ(272件・約4時間17分)は再起動を挟まず単発で連続実行した2回目の試行のもの**であり、再起動イベント自体はこのデータには含まれていない。272件の`[PERF-SPIKE]`発火をloop番号で10等分して分布を確認したところ、18〜42件/10分割と全区間にわたって発生しており、特定区間(開始直後・終了直前等)への偏りは見られなかった(初回サイクルのcache_builds_diff=20の1件のみreference_path初期構築という既知の正常な起動時イベント)。よって272件・4時間17分は定常状態を代表するデータとして扱ってよい。
- **CFSスロットリングの確認(2026-08-01追加、Phase 2)**: `autoware`コンテナ(`cpus:4`クォータ、cgroup v2)の`cpu.stat`を起動直後・約7分後で比較した結果、`nr_throttled`は起動バーストの13回(8.15秒)のみで、定常72Hz運転の7分間では**1件も増加しなかった**。これはCFSクォータスロットリングが定常時の残存スパイクの原因ではないことを示す直接証拠であり、仮説(b)の内訳は「クォータ超過による強制停止」ではなく、より一般的な「OSスケジューラによる横取り(nivcswに現れる)」であると絞り込まれた。

結論: 仮説(b)「CPU競合」がスパイクの主因であることは変わらないが、CFSクォータスロットリングは否定され、その内訳は「simulatorとの競合」よりも「同一コンテナ内の複数ROS2ノード間競合」の寄与が無視できない、という新しい仮説が浮上した。単純なgovernor固定・単純なコンテナ単位cpuset隔離のいずれも、4基準を全て満たすには至らなかった。詳細は design_docs/stage15_perf_20260707.html 262節続報を参照。

**72Hz(C1)は絶対遅延の観点で既に40Hzより優位(2026-08-01追加)**: 判定基準(4基準AND)の合否とは別に、実際の反応遅延の絶対値で比較すると、C1(72Hz、governor=performance)はdt平均≈1/70.58Hz≈14.17ms・p99=28.84msであるのに対し、Phase 4の40Hzベースラインはdt平均=25.00ms(eff_rate=40.00Hzちょうど)・p99=42.54msであり、**平均・p99とも72Hz(C1)の方が約1.4〜1.8倍高速に反応している**。72Hz化の本来の目的(反応速度向上)自体は、4基準の合否とは独立に、この絶対値比較の観点で既に達成方向にあることを記録しておく。

**workは壁時計時間であることの明記と、work_cpu計装の方針(2026-08-01追加)**: `[PERF]`/`[PERF-SPIKE]`のwork(処理時間)は`time.perf_counter()`(壁時計)で測っており、スケジューラに横取りされて待たされた時間も含む。「計算量が増えた」のか「CPUを取り上げられて待たされた」のかを区別するため、`time.thread_time()`(呼び出しスレッドのCPU時間)との差分を`work_cpu`として並行計装した(`[PERF-SPIKE]`行・`[PERF]`窓集計へ追加、262節続報Phase 1)。**work基準(0.1%)は当面wallのまま維持する**——制御応答の観点で意味を持つのは実際に経過した壁時計時間であり、CPU時間ベースへ切り替えると「計算自体は速いがスケジューラ待ちで実際には遅れている」状態を誤って合格にしてしまうため。work_cpuはあくまで残存スパイクの帰属診断(スケジューラ起因か計算起因か)に用いる補助指標と位置づける。なお呼び出しスレッド単体のCPU時間のため、OSQP/numpyのBLASが内部で複数スレッドを使う場合はヘルパースレッド分を含まず、健全な高負荷サイクルでも`work_cpu`が`work`を過小評価しうる(解釈時は`[PERF-RUSAGE]`のプロセス全体`cpu_ratio`と突き合わせること)。

**2026-08-01: Phase 4 — 新プラットフォーム状態での40Hzベースライン再取得**

`control_rate`を40.0へ復帰し、Phase 3で最良だった条件(C1: `governor=performance`、cpuset隔離なし)と同一のプラットフォーム設定で、単一車両約9.92分(59窓・23,600周期)のベースラインを再取得した。

| 指標 | 実測値 |
|---|---|
| work>25ms(40Hz予算)割合 | 0.000%(23,600周期中0回) |
| work max(窓最悪) | 22.20ms |
| dt p99(窓平均) | 42.54ms |
| dt p99(窓ごと最悪) | 54.58ms |
| dt max(窓平均) | 45.57ms・(窓ごと最悪)59.55ms |
| eff_rate | 40.00Hz(想定通り) |
| `[PERF-SPIKE]`(factor=2.0、40Hzなら>50ms) | 0件 |
| freq_avg(窓平均) | 3834MHz |
| nivcsw/720周期窓平均 | 2031.9 |

処理時間(work)は完全に健全(予算超過0%)である一方、dt(実際の呼び出し間隔)のp99は予算25msの約1.7倍(42.54ms)に達する。これは手順7の脚注通り、処理落ちではなく`rclpy.Rate.sleep()`自体のジッタが支配的であるという既存の理解と整合する。

**ε再較正の判断: 現行ε=85msを維持し、引き下げは見送る**。今回の単一車両ベースライン単独からε算出式(窓ごと最悪p99−予算に1.3倍の余裕)を適用すると (54.58−25.0)×1.3 ≈ 38.5ms となり現行の85msより緩められる余地があるように見えるが、既存のε=85msは対戦車あり2両編成(dev2、旧プラットフォーム状態)の方がより負荷が重いという実測(手順11の②)に基づいて較正されたものであり、**新プラットフォーム状態(governor=performance)での対戦車ありベースラインは未取得**である。単一車両データのみで下方修正すると、対戦車ありの実際の負荷を過小評価するリスクがあるため、対戦車ありの再測定を行うまでは現行ε=85msを保守側の値として維持する(実質的な検査として機能しなくなるほど緩すぎるわけではなく、今回の実測でも参考集計上の連続超過は0件のまま)。対戦車ありでの再較正は今回のPhase 4の範囲外とし、必要になった時点の別課題とする。

**2026-08-01: Phase 5 — C3実験(mpc_controllerプロセス単体アフィニティ+コンテナクォータ緩和)**

C2(コンテナ全体cpuset隔離)の失敗を踏まえ、`autoware`コンテナの`cpus`クォータを16(実質無制限)へ緩和した上で、`mpc_controller`プロセス自身のみを`os.sched_setaffinity`で論理CPU2-3(物理コア1つ)へ固定するC3構成を実装・実測した(`docker-compose.c3-experiment.yml`+`config.yaml`の`mpc.cpu_affinity: [2, 3]`)。`[PERF-PLATFORM]`ログ・`/proc/<pid>/status`のCpus_allowed_listいずれでも`2-3`が実際に効いていることを確認した上で計測した。

*40Hzミニベースライン(C3構成、約9分)*: work>25ms割合**0.194%**(Phase 4の無介入時0.000%から悪化)、dt p99=45.11ms(J≈20.11ms、無介入時のJ≈17.5msより悪化)。**物理コア1つ(論理2コア)へ固定するとmpc_controller自身の内部マルチスレッド計算(OSQP/numpyのBLAS)がSMT兄弟スレッド間で自己競合し、40Hzという軽負荷ですら健全性が悪化する**ことが判明した(前述の`[PERF-RUSAGE]`cpu_ratioが1.7〜2.9(1コア分を超えるCPU時間を消費)だった実測と整合する)。

*72Hz本実測(C3構成、C1と同一長の約11.02分)*:

| 指標 | 目標 | C0 | C1 | C2 | C3(cpu_affinity=[2,3]) |
|---|---|---|---|---|---|
| work>13.9ms割合 | ≦0.1% | 0.823% | 0.684% | 0.917% | **0.946%(最悪)** |
| dt p99(窓平均) | ≦31.4ms(改訂基準) | 28.43ms✓ | 28.84ms✓ | 33.56ms❌ | 29.95ms✓ |
| 連続超過 | ≦3周期 | 0✓ | 0✓ | 0✓ | 0✓ |
| eff_rate | ≧70Hz | 70.43Hz✓ | 70.58Hz✓ | 62.17Hz❌ | 70.33Hz✓ |
| nivcsw/720周期窓平均 | (参考) | 未計測 | 2304.4 | 4268.3 | **1517.5(最良、C1比−34%)** |
| freq_avg(窓平均) | (参考) | 未計測 | 3749MHz | 3477MHz | **4096MHz(最高)** |
| `[PERF-SPIKE]`発火頻度 | (参考) | 3.7件/分 | 0.74件/分 | 1.06件/分 | 1.18件/分 |

改訂dt p99基準(予算+J、J≈17.5ms)で判定すると、C0・C1・C3は**dt p99基準を満たし、4基準中3基準に合格**(未達は`work>13.9ms割合`の1基準のみ)。C2のみdt p99も未達で1/4に留まる。

**work_cpu計装による直接確認**: 発火した13件全ての`[PERF-SPIKE]`行でwork≫work_cpu(例: work=28.50ms・work_cpu=7.37ms、平均ギャップ21.43ms)であり、**残存する`work>予算`スパイクは計算量の増加ではなくスケジューラによる横取り待ちであることが直接確認された**(仮説(b)の帰属を工数の推測ではなく実測で裏付けた、262節続報Phase 1で導入したwork_cpu計装の初成果)。

**C3の評価**: nivcsw(外部競合の代理指標)はC1比−34%、freq_avgも最高値——mpc_controllerを専有コアへ固定してsimulator等他プロセスとの競合を減らす狙い自体は機能している。しかし`work>13.9ms割合`はC0/C1/C2いずれよりも悪化し、4条件中最悪だった。これは40Hzミニベースラインで確認した通り、**論理2コア(物理1コア)という割当量そのものがmpc_controller自身の内部並列計算には狭すぎ、「外部競合の低減」という利得を「内部自己競合の新規発生」という損失が上回った**ためと考えられる。C3という**方式**(コンテナ全体ではなくプロセス単体をアフィニティで隔離する)は正しい方向性だが、**今回のコア数(2)という具体的なパラメータが不適切だった**可能性が高い。

**未実施の次候補(提案のみ)**: `cpu_affinity`をより広く(例: 論理4コア=物理2コア、`[2, 3, 4, 5]`)取り、mpc_controller自身の内部並列度を確保しつつ他ノード・simulatorとは依然分離する設定を試す価値がある。ただし本Phase 5の範囲外とし、実装・実測は人間の承認を経てから次ラウンドで行う。

**最終状態**: `control_rate=40.0`・`mpc.cpu_affinity=[]`(共に復帰済み)、`docker-compose.c3-experiment.yml`は実験用ファイルとして残す(`docker-compose.override.yml`は未使用)。72Hz化は**引き続き不採用**——4基準中3基準は複数条件(C0/C1/C3)で達成可能だが、`work>13.9ms割合≦0.1%`の1基準のみが一貫して未達であり、work_cpu計装によりこれがスケジューラ横取りに起因することが直接確認されている。

## 7. 未解決のリスク・正直な限界

- 計算負荷(処理落ち率)は実測するまで確定しない。72Hzが実運用環境で成立するかどうかは、本設計の実装だけでは保証できない
- `steer_low_pass_gain`は蛇行対策として過去に慎重に手動チューニングされた値であり、指数換算式で機械的に算出した値(0.35→**0.213**、検算式: `1-(1-0.35)^(40/72)=0.2128`。旧版の本書に「0.231」との誤記があったが2026-07-31のGeminiレビューで指摘され訂正した)が同等に機能するかは実地での再検証が必要
- 本設計は「反応速度」の向上のみを目的としており、蛇行(AXIS06)への効果は見込んでいない。これを混同して評価しないこと
- **Phase 4(40Hz恒等性の実機ログ確認)は本セッションのサンドボックス環境では実施できなかった**: このリポジトリのROS2ワークスペース(`install/`)のシンボリックリンクが別環境(コンテナ)の絶対パスを指しており、`get_package_share_directory`がパッケージを解決できない(未ビルド相当)。数式の恒等性(40Hz時に入力=出力)は単体テストで証明済み、また回帰スイート全体(既存1213件、新規20件、計1233件)がPASSし挙動に変化がないことも確認済みだが、実際にノードを起動して`[RATE-SCALE]`ログの実出力を目視確認する作業は、正しくビルドされた開発/実運用環境で改めて実施する必要がある(ロールアウト手順1に相当)

### 実装時に判明した追加事項(design書レビュー時点では未確定だった詳細)

- **`osqp_shadow_cycles`のみ、ヘルパーが`self._mpc_cfg.control_rate`ではなく`self._cfg.mpc.control_rate`を参照する**: `osqp_shadow_cycles`は`create_mpc()`内、すなわち`self._mpc_cfg`へ代入される**前**に読まれるため、当初の設計通り`self._mpc_cfg.control_rate`を参照すると初期化順序でAttributeErrorになることが実装時に判明した。両ヘルパー(`_rate_scaled_cycles`/`_rate_scaled_gain`)は、`__init__`時点から常に利用可能な生の設定値`self._cfg.mpc.control_rate`(`self._mpc_cfg.control_rate`と同一値)を参照するよう実装した。この変更は全23箇所に一貫して適用されており、挙動への影響はない
- **`space_ema_alpha`(LateralTTCMonitorのコンストラクタ引数)はヘルパーでラップしない**: config.yamlに明示的な`space_ema_alpha`キーは存在せず、コード上のデフォルト値は`self._ot_ema_alpha`(既にヘルパーで換算済み)への参照になっている。ここをさらにヘルパーでラップすると、通常運用時(config.yamlに明示設定がない場合)に二重換算になってしまうため、意図的にラップ対象から除外した
- **`accel_low_pass_gain`/`steer_low_pass_gain`のROS2動的パラメータコールバック(`ros2 param set`によるライブ変更)はヘルパーでラップしない**: これらは起動後に運用者が値を直接変更できる別経路であり、変更時点で運用者が意図する値は「現在アクティブなcontrol_rateでの値」であって「40Hz基準の値」ではないと考えられるため、config.yaml読み込み時のみを対象とする本設計のスコープ外とした
- **動的パラメータ経路の意味論(2026-07-31追加、クローズ作業Phase 1)**: 上記除外により、`accel_low_pass_gain`/`steer_low_pass_gain`には「config.yaml経由(40Hz基準・`control_rate`に応じて自動換算)」と「`ros2 param set`経由(現在の`control_rate`における生値、換算なし)」という**意味の異なる2つの設定経路**が存在することになる。`control_rate`が40Hzのままの間はこの差が顕在化しないが、72Hzへ切替後に運用者がこの違いに気づかず`ros2 param set`で値を調整すると、意図と異なる実効時定数になりうる。この混同を防ぐため、両パラメータの動的コールバック内に`control_rate != 40Hz`の場合のみ発火する1回警告(`_warn_if_dynamic_gain_param_unscaled`)を追加した(換算はしない、生値適用のまま、ログ追加のみで挙動は変えない)
