x-autoware-base: &autoware-base
  image: "aichallenge-2025-dev"
  privileged: true
  pull_policy: never
  network_mode: host
  stop_grace_period: 10s
  user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
  environment:
    - HOME=/tmp
    - DISPLAY=${DISPLAY}
    - USER=${USER}
    - ROS_DISTRO=humble
    - XAUTHORITY=${XAUTHORITY:-}
    - QT_X11_NO_MITSHM=1
    - TZ=Asia/Tokyo
    - RUN_MODE=${RUN_MODE:-}
    - SIM_MODE=${SIM_MODE:-}
    - CMD=${CMD:-}
    - COLCON_TRACE=${COLCON_TRACE:-}
    - ROS_HOME=${ROS_HOME:-}
    - ROS_LOG_DIR=${ROS_LOG_DIR:-}
    - HOST_UID=${HOST_UID:-}
    - HOST_GID=${HOST_GID:-}
    - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-1}
    - LOG_DIR=${LOG_DIR:-}
    # 2026-08-09追加: 再起動後、AWSIM(Unity/Mesa)が誤ってAMD内蔵GPU(Rembrandt iGPU)側の
    #   renderD128を掴みFPSが50-60→25まで低下する事象を確認。DRI_PRIME=1でNVIDIA discrete
    #   GPU(renderD129)を強制指定し復旧(クロック210MHz→1400MHz超、使用率100%)。
    - DRI_PRIME=${DRI_PRIME:-1}
  volumes:
    - ./output:${OUTPUT_ROOT:-/output}
    - ./aichallenge:/aichallenge
    - ./remote:/remote
    - ./vehicle:/vehicle
    - ./vehicle/cyclonedds.xml:/opt/autoware/cyclonedds.xml
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    - /dev/dri:/dev/dri
    - ${XAUTHORITY:-/dev/null}:${XAUTHORITY:-/dev/null}:rw
    - /etc/passwd:/etc/passwd:ro
    - /etc/group:/etc/group:ro
  devices:
    - /dev/dri
    - /dev/video0
    - /dev/input
  group_add:
    # Read /dev/input/event* (joy_node/SDL2) as non-root user. GID from host `input` group.
    - "${HOST_GID_INPUT:-104}"
  working_dir: /aichallenge

x-autoware-rosbag-base: &autoware-rosbag-base
  <<: *autoware-base
  stop_grace_period: 60s
  volumes:
    - ./output:${OUTPUT_ROOT:-/output}
    - ./aichallenge:/aichallenge
    - ./remote:/remote
    - ./vehicle:/vehicle
    - ./vehicle/cyclonedds.xml:/opt/autoware/cyclonedds.xml
    - ${RACING_KART_INTERFACE_DIR:-/home/tier4/racing_kart_interface}:${RACING_KART_INTERFACE_DIR:-/home/tier4/racing_kart_interface}:ro
    - /etc/passwd:/etc/passwd:ro
    - /etc/group:/etc/group:ro
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    # No audio mount: rosbag recording needs no PulseAudio.
    - /dev/dri:/dev/dri
    - ${XAUTHORITY:-/dev/null}:${XAUTHORITY:-/dev/null}:rw

# 2026-07-24: 提出(予選/決勝)環境の実測リソース制約(Autowareコンテナに約3 vCPU・約12GBメモリ、
#   当時の推定値)をローカルでも再現するための共通リソース制限として cpus=3/mem=12g を導入した。
#   Autowareコンテナ(制御本体)にのみ適用し、simulator(AWSIM)やビルドは制限しない(提出環境でも
#   Autoware以外は制限されていないため)。
# 2026-07-25単独実験(179節続報): CPU競合ノイズを除去する目的で一時的にcpus=8/mem=24gへ緩めた。
# 2026-07-25再校正(180節、実測データに基づく本校正): 実際の予選ログ(0725-01)を計装(mpc_setup/
#   solve/linearize/corridor分離、PERF-RUSAGE)付きで実測した結果、超過率は平均1.5%(0-7.8%)・
#   work avg平均10.2msと、ローカルdev3(cpus=3/mem=12g)で観測していた超過率29.5-52.75%・
#   work avg21.7-38.6msを大幅に下回っていた。つまり2026-07-24時点の「約3vCPU」という推定値は
#   過大に厳しく、実環境の負荷を大幅に過大評価していたと判明した。ホスト実機(16コア/30GB、
#   179節でdocker stats実測済み)に対しdev3(3台自己対戦)が同時起動することを踏まえ、
#   cpus=4/mem=8g(3台合計12コア/24GB、host比75-80%、simulator/OS分の余裕を残す)へ再校正する。
#   これは実測データに基づく第一次校正であり、次回dev3ログの超過率が実環境の1.5%程度に
#   近づくか確認し、必要なら追加調整すること。
x-autoware-resource-limits: &autoware-resource-limits
  cpus: "4"
  mem_limit: "8g"

x-racing_kart_interface-base: &racing_kart_interface-base
  image: "ghcr.io/tier4/racing_kart_interface:latest-experiment"
  privileged: true
  pull_policy: never
  network_mode: host
  stop_grace_period: 10s
  user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
  environment:
    - HOME=/tmp
    - DISPLAY=${DISPLAY}
    - USER=${USER}
    - TZ=Asia/Tokyo
    - ROS_DISTRO=humble
    - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-1}
    - LOG_DIR=${LOG_DIR:-}
    - HOST_UID=${HOST_UID:-}
    - HOST_GID=${HOST_GID:-}
    - NTRIP_USERNAME=${NTRIP_USERNAME:-}
    - NTRIP_PASSWORD=${NTRIP_PASSWORD:-}
  volumes:
    - ./output:${OUTPUT_ROOT:-/output}
    - ./vehicle:/vehicle:ro
    - ./aichallenge/utils:/aichallenge/utils:ro
    - /dev/vcu:/dev/vcu
    - /dev/gnss:/dev/gnss
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    - /dev/dri:/dev/dri
    - /etc/passwd:/etc/passwd:ro
    - /etc/group:/etc/group:ro
  devices:
    - /dev/dri
  group_add:
    - "${HOST_GID_DIALOUT:-20}"
  working_dir: /workspace

# 2026-08-05復元(291節続報): origin/mainマージ時、upstreamがこのアンカー定義を
#   持たない構成(docs/spec/等へ再構成)だったため、3-wayマージが本ブロックの定義
#   (非conflict扱いの箇所)を静かに削除していたことが判明(autoware-d1~d4が参照する
#   *autoware-eval-base が未定義になりmake dev3が起動不能)。b499125時点の内容を
#   そのまま復元する。
x-autoware-eval-base: &autoware-eval-base
  <<: *autoware-base
  volumes:
    - ./output:${OUTPUT_ROOT:-/output}
    # /aichallenge is baked into the eval image; intentionally not mounted here
    # - ./aichallenge:/aichallenge
    - ./vehicle/cyclonedds.xml:/opt/autoware/cyclonedds.xml
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    # Audio (PipeWire/Pulse) socket access on the host
    - /run/user:/run/user:rw
    - /dev/dri:/dev/dri
    - ${XAUTHORITY}:${XAUTHORITY}:rw

services:
  # Autoware service
  autoware:
    <<: [*autoware-base, *autoware-resource-limits]
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} ${ROS_DOMAIN_ID:-1} ${LOG_DIR:-}"]

  # Autoware service
  autoware-build:
    <<: *autoware-base 
    command: ["bash", "-lc", "exec /aichallenge/build_autoware.bash ${HOST_UID:-} ${HOST_GID:-}"]

  simulator:
    <<: *autoware-base 
    command: ["bash", "-lc", "exec /aichallenge/run_simulator.bash"]

  autoware-command:
    <<: *autoware-base
    command: ["bash", "-lc", "cd ${CMD_WORKDIR:-/aichallenge} && exec bash -lc \"${CMD:-}\""]

  # bag-recorder: 自作Makefileターゲット(dev/dev2/dev3/dev4)が参照する既存サービス、維持。
  bag-recorder:
    <<: *autoware-base
    command: ["bash", "-lc", "exec /aichallenge/utils/record_dev_bag.bash ${BAG_TAG:-dev} > \"${LOG_DIR:-/output}/rosbag.log\" 2>&1"]

  # rosbag: origin/main新設(全トピック記録用)。既存bag-recorderと併存、未使用時は無害。
  rosbag:
    <<: *autoware-rosbag-base
    command: ["bash", "-lc", 'exec /aichallenge/utils/record_all_rosbag.bash "${ROS_DOMAIN_ID:-1}" "${LOG_DIR:-}" "${RACING_KART_INTERFACE_DIR:-/home/tier4/racing_kart_interface}"']

  autoware-simulator-evaluation:
    <<: *autoware-base
    image: "aichallenge-2025-eval"
    # /aichallenge is baked into the eval image; intentionally not mounted here.
    volumes:
      - ./output:${OUTPUT_ROOT:-/output}
      - ./vehicle/cyclonedds.xml:/opt/autoware/cyclonedds.xml
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /dev/dri:/dev/dri
      - ${XAUTHORITY:-/dev/null}:${XAUTHORITY:-/dev/null}:rw
      - /etc/passwd:/etc/passwd:ro
      - /etc/group:/etc/group:ro
    command: ["bash", "-lc", "exec /aichallenge/run_evaluation.bash"]

### Autoware Service For Parallel Execution ###
  autoware-d1:
    <<: [*autoware-eval-base, *autoware-resource-limits]
    image: "autoware-d1"
    command: ["bash", "-lc", "exec env ROS_DOMAIN_ID=1 ros2 launch aichallenge_system_launch aichallenge_system.launch.xml simulation:=true use_sim_time:=true run_rviz:=true domain_id:=1 > \"${LOG_DIR:-/output}/autoware.log\" 2>&1"]

  autoware-d2:
    <<: [*autoware-eval-base, *autoware-resource-limits]
    image: "autoware-d2"
    command: ["bash", "-lc", "exec env ROS_DOMAIN_ID=2 ros2 launch aichallenge_system_launch aichallenge_system.launch.xml simulation:=true use_sim_time:=true run_rviz:=true domain_id:=2 > \"${LOG_DIR:-/output}/autoware.log\" 2>&1"]

  autoware-d3:
    <<: [*autoware-eval-base, *autoware-resource-limits]
    image: "autoware-d3"
    command: ["bash", "-lc", "exec env ROS_DOMAIN_ID=3 ros2 launch aichallenge_system_launch aichallenge_system.launch.xml simulation:=true use_sim_time:=true run_rviz:=true domain_id:=3 > \"${LOG_DIR:-/output}/autoware.log\" 2>&1"]

  autoware-d4:
    <<: [*autoware-eval-base, *autoware-resource-limits]
    image: "autoware-d4"
    command: ["bash", "-lc", "exec env ROS_DOMAIN_ID=4 ros2 launch aichallenge_system_launch aichallenge_system.launch.xml simulation:=true use_sim_time:=true run_rviz:=true domain_id:=4 > \"${LOG_DIR:-/output}/autoware.log\" 2>&1"]

#### vehicle specific services can be added here ####
  # Zenoh service
  zenoh:
    <<: *autoware-base
    working_dir: /vehicle
    command: ["bash", "-lc", "exec /vehicle/run_zenoh.bash ${VEHICLE_ID:-} ${ROS_DOMAIN_ID:-1} ${LOG_DIR:-}"]

  # Driver service
  driver:
    <<: *racing_kart_interface-base
    entrypoint: []
    command: ["bash", "-lc", "exec /vehicle/run_driver.bash vehicle ${ROS_DOMAIN_ID:-1} ${LOG_DIR:-}"]

#### remote visualization services ####
  rviz2:
    <<: *autoware-base 
    command: ["bash", "-lc", "exec /aichallenge/utils/run_rviz.bash remote"]
